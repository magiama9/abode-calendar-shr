import React, { useState, useEffect } from 'react';

function useEventFormState(eventFormState) {
  const [workingEventState, setEventFormState] = useState({ title: '' });

  function handleEventFormStatus {
    setWorkingEventState(eventFormState)
  }

  useEffect(()=>{
    
  })
}
